apply plugin: 'spring-boot'

bootRun {
    args = ["--spring.profiles.active=stg"]
}

task setProdProperties(dependsOn: bootRun) << {
    doFirst {
        System.setProperty('spring.profiles.active', 'stg')
    }
}